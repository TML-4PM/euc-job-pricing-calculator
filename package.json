{
  "name": "wasted-weekend",
  "version": "1.0.0",
  "description": "Job pricing calculator for Officeworks & Geeks2U",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p public && cp -r src/* public/",
    "deploy": "npm run build && netlify deploy"
  },
  "dependencies": {
    "express": "^4.18.2",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.0.3",
    "nodemailer": "^6.9.1"
  },
  "devDependencies": {
    "netlify-cli": "^15.5.0"
  },
  "engines": {
    "node": ">=16"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TML-4PM/wasted-weekend.git"
  },
  "license": "MIT",
  "private": true
}
